import NotificationsPanelContent from '../components/NotificationsPanelContent.jsx'

// The sidebar's "Notifications" page. It reuses the same panel body as
// the dropdown so the design stays identical wherever it is shown.
const Notifications = () => {
  return <NotificationsPanelContent width="100%" maxHeight="none" />
}

export default Notifications
